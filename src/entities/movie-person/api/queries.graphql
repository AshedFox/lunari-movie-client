query GetMoviePersons($movieId: String!, $limit: Int!, $offset: Int!) {
  getMoviesPersons(
    limit: $limit
    offset: $offset
    filter: { movieId: { eq: $movieId } }
  ) {
    nodes {
      ...MoviePerson
    }
    pageInfo {
      hasNextPage
    }
  }
}
