fragment UserProfile on User {
  id
  name
  email
  isEmailConfirmed
  role
  createdAt
  updatedAt
  country {
    id
    name
    currencyId
  }
  avatar {
    id
    url
  }
}

fragment ShortUser on User {
  id
  name
  avatar {
    id
    url
  }
}
